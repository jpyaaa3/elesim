schema_version: 1
extends: config.yaml

# Remote compute profile: render both network cameras without opening a local
# Genesis desktop viewer on the simulation host.
simulation:
  runtime:
    use_gpu: true
    enable_viewer: false
  cameras:
    hand_eye:
      enable: true
    observer:
      enable: true
