# Vision Scenario

These tests follow the headless perception path before any robot motion is
trusted: config, hand-eye transform, synthetic world mapping, remote config,
lifecycle, detector/tracker, then sim-camera contracts.
