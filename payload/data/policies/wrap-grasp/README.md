# Wrap-grasp policy slot

Place one compatible exported policy set here:

- `policy.pt`: Pilot runtime artifact
- `policy.npz`: optional NumPy/reference artifact
- `interface.json`: bounded observation/action contract

The artifacts are generated by `elesim_sim.rl.export`. They are data consumed
by Pilot; Pilot does not import Sim's training or export implementation.
