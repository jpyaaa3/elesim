# Regressions

Regression tests pin known symptoms after the root cause has been understood.
They are intentionally narrower than scenarios and more concrete than
contracts.

- `pick/`: pick log fixtures such as LJI sign flips, equal-sag gate behavior,
  and blind handoff thresholds.

