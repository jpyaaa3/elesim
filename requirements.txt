# Core (host / ctrl / engine)
numpy>=2.0
scipy>=1.11
pyzmq>=27.0

# UI (ctrl.py control panel)
glfw>=2.8
imgui[glfw]>=2.0

# Hardware (host.py serial + Dynamixel)
pyserial>=3.5
dynamixel-sdk>=4.0

# Simulation (sim.py)
genesis-world>=0.4.7

# Perception (camera, hand-eye, visual tracking)
opencv-python>=4.8
pyrealsense2>=2.54

# YOLO detector (optional; camera mode with --detector yolo)
ultralytics>=8.0
